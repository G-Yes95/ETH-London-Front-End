const TurnkeyErrorMap = {
    0: 'Operation was canceled.',
    16: 'Invalid Passkey signature. Select the passkey for this account and device.',
    3: 'You reached the limit of passkeys for this account.',
};
const DOMExceptionErrorMap = {
    NotAllowedError: 'Make sure to have you browser tab focused.',
};
const getProperErrorMessage = (originalError) => {
    var _a, _b;
    let error = originalError.reason || 'Something went wrong';
    if (originalError instanceof DOMException) {
        return DOMExceptionErrorMap[originalError.name] || error;
    }
    // turnkey request errors
    const code = ((_a = originalError === null || originalError === void 0 ? void 0 : originalError.cause) === null || _a === void 0 ? void 0 : _a.code) !== undefined
        ? (_b = originalError === null || originalError === void 0 ? void 0 : originalError.cause) === null || _b === void 0 ? void 0 : _b.code
        : originalError === null || originalError === void 0 ? void 0 : originalError.code;
    if (code !== undefined) {
        error = TurnkeyErrorMap[code] || error;
    }
    return error;
};

export { getProperErrorMessage };
