import { ReactNode } from 'react';
type Variant = 'error' | 'info' | 'success';
type Props = {
    children: ReactNode;
    className?: string;
    isMultiWalletError?: boolean;
    variant?: Variant;
    withIcon?: boolean;
};
export declare const ErrorContainer: ({ children, className, isMultiWalletError, withIcon, variant, }: Props) => JSX.Element;
export {};
