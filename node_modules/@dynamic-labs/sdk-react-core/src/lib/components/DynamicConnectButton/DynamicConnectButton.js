import { __awaiter } from '../../../../_virtual/_tslib.js';
import { jsx } from 'react/jsx-runtime';
import { useState, useCallback } from 'react';
import { useInternalDynamicContext } from '../../context/DynamicContext/useInternalDynamicContext.js';
import { useViewContext } from '../../context/ViewContext/ViewContext.js';
import { ConnectButton } from './components/ConnectButton.js';

const DynamicConnectButton = ({ buttonContainerClassName = '', buttonClassName = '', children, }) => {
    const { isProjectSettingsLoading, setShowAuthFlow, setSelectedWalletConnector, } = useInternalDynamicContext();
    const { setView } = useViewContext();
    const [didUserClick, setUserClicked] = useState(false);
    const handleOnClick = useCallback(() => __awaiter(void 0, void 0, void 0, function* () {
        if (!didUserClick) {
            setUserClicked(true);
        }
        setSelectedWalletConnector(null);
        setShowAuthFlow(true);
    }), [didUserClick, setSelectedWalletConnector, setShowAuthFlow, setView]);
    return (jsx(ConnectButton, { isLoading: isProjectSettingsLoading, isActive: didUserClick, onClick: handleOnClick, buttonContainerClassName: buttonContainerClassName, buttonClassName: buttonClassName, children: children }));
};

export { DynamicConnectButton };
