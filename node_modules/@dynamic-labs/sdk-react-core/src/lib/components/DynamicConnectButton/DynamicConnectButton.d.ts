import { ReactNode } from 'react';
type Props = {
    buttonClassName?: string;
    buttonContainerClassName?: string;
    children: ReactNode;
};
export declare const DynamicConnectButton: ({ buttonContainerClassName, buttonClassName, children, }: Props) => JSX.Element;
export {};
