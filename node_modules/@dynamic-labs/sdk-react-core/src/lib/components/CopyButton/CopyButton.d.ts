import { ReactNode, ComponentPropsWithoutRef } from 'react';
type Props = ComponentPropsWithoutRef<'button'> & {
    children: ReactNode;
    className?: string;
    onClick?: VoidFunction;
    textToCopy: string;
};
export declare const CopyButton: ({ children, className, textToCopy, onClick, }: Props) => JSX.Element;
export {};
