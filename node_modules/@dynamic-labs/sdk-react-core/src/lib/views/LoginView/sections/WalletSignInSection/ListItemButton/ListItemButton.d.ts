/// <reference types="react" />
type Props = {
    numberOfWallets: number;
    onClick: VoidFunction;
    text: string;
    disabled?: boolean;
};
export declare const ListItemButton: ({ text, onClick, numberOfWallets, disabled, }: Props) => JSX.Element;
export {};
