import { GenericNetwork } from '@dynamic-labs/types';
export type fetchNetworkConfigurationsProps = {
    authToken: string | undefined;
    environmentId: string;
    evmNetworks?: GenericNetwork[];
    maxRetries?: number;
};
export declare const useFetchNetworkConfigurations: ({ authToken, environmentId, evmNetworks: customerEvmNetworks, maxRetries, }: fetchNetworkConfigurationsProps) => {
    configurations: import("@dynamic-labs/types").NetworkConfigurationMap | undefined;
    handleRemoveLsNetworks: () => void;
};
