import { FallbackProvider, JsonRpcProvider, BrowserProvider, JsonRpcSigner } from 'ethers';
import { WalletConnectorExtension } from '@dynamic-labs/wallet-connector-core';
declare module '@dynamic-labs/wallet-connector-core' {
    namespace WalletConnectorCore {
        interface WalletConnector {
            ethers?: IEthersExtension;
        }
        interface IEthersExtension {
            getRpcProvider(): Promise<FallbackProvider | JsonRpcProvider>;
            getSigner(): Promise<JsonRpcSigner>;
            getWeb3Provider(): Promise<BrowserProvider>;
        }
    }
}
export declare const EthersExtension: WalletConnectorExtension;
